v0.20-experimental

- Removed ~feed
- Changed ~shodan-search to ~shodan
- Fixed ~help returning 'none' when no specific command was specified.
- Removed ~emergfixes
- Comment cleanup
- Removed feedreader dependency
- Removed libraries that are no longer required.

==========Experimental changes:

% Discord error handling has been 
  moved to a ```def``` statement

% Error handling for fetching
  CVE data (e.g. exploit-db data)
  has been moved to a ```def```
  statement. 

% Changed the "ready" status to
  "Discord Bot Ready!"

All experimental changes are
expected to adversely affect
the stability of the script. 

Please take care when using
this version.